import { Hover, Position } from "vscode-languageserver";
import { IArgdownResponse } from "@argdown/core";
export declare const provideHover: (response: IArgdownResponse, position: Position) => Hover;
