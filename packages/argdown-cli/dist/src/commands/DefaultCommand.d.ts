import { Arguments } from "yargs";
export declare const command = "*";
export declare const desc = "load config file and run process";
export declare const handler: (argv: Arguments) => Promise<void>;
